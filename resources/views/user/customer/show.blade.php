<div class="mt-3 border-b p-3 flex items-center">
    <div class="w-1/2 border-r">{{ __('avored::system.first_name') }}</div>
    <div class="w-1/2 ml-5">{{ $customer->first_name }}</div>
</div>
<div class="mt-3 border-b p-3 flex items-center">
    <div class="w-1/2 border-r">{{ __('avored::system.last_name') }}</div>
    <div class="w-1/2 ml-5">{{ $customer->last_name }}</div>
</div>
<div class="mt-3 border-b p-3 flex items-center">
    <div class="w-1/2 border-r">{{ __('avored::system.email') }}</div>
    <div class="w-1/2 ml-5">{{ $customer->email }}</div>
</div>
